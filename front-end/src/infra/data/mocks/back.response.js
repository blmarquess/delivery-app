// retornos /Login
// success:
// Send:
// { email, password }
// Response: success:
// { id, name, token, role },
// Response: failure:
// { status, message: 'Invalid email or password' },

// rota /validate:
// success:
// { token }
// responder:
// { valido, role }
